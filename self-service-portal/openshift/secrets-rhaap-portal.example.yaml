# Example only — do NOT apply with real credentials. Use scripts/bootstrap-secrets.sh instead.
apiVersion: v1
kind: Secret
metadata:
  name: secrets-rhaap-portal
  namespace: aap
type: Opaque
stringData:
  aap-host-url: "https://demo-aap-aap.apps.example.com"
  oauth-client-id: "REPLACE_OAUTH_CLIENT_ID"
  oauth-client-secret: "REPLACE_OAUTH_CLIENT_SECRET"
  aap-token: "REPLACE_AAP_PERSONAL_ACCESS_TOKEN"
