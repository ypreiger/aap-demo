---
# Workstation installs. Automation Controller installs
# collections/requirements.yml from the **repository root** (AAP project cwd).
# Keep this list a subset; the full previous-demo BOM is in
# ../../collections/requirements.yml (Hub community sync + project install_collections).
collections:
  - name: kubernetes.core
    version: ">=3.0.0"
  - name: community.general
    version: ">=8.0.0"
