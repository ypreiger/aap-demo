---
# Naming convention: {{ namespace }}-sa (namespace is proj1 -> proj1-sa).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: proj1-sa
  namespace: proj1
  labels:
    app.kubernetes.io/managed-by: aap-demo
