# Agent: Workshop documentation & presenter guide

## Deliverables

- Keep **`workshop/README.md`** as the index; cross-link `documentation/*` for deep dives.
- Each **`workshop/use-cases/USECASE_*.md`** must list **inputs**, **Controller objects**, **expected artifacts**, **rollback**.

## Style

- Prefer imperative steps + code blocks for copy/paste.
- Call out **approval + email** timing (webhook JSON sample in `email-plugin/README.md`).
